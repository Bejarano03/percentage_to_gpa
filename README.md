# percentage_to_gpa

## Description

This program takes in a percentage in decimal format and
returns a GPA value from a 0.0 to 4.0 scale. The user is
prompted for a value and then it returns the corresponding
value.

## Installation

- Make sure you have a local Java build
- Clone repo to local machine
- Navigate to directory and run Main java class